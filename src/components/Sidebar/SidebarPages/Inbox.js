import { useSelector } from "react-redux";

const Inbox = () => {
  // const sentDatas = useSelector((state) => state.userData.sentDatas);
  // const sentEmail = useSelector((state) => state.userData.sentEmail);

  return <div className="mx-4 mt-2 border w-4/6">This is Inbox</div>;
};

export default Inbox;
